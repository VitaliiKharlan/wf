import 'package:flutter/material.dart';

class GreenScreenWidget extends StatelessWidget {
  const GreenScreenWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
